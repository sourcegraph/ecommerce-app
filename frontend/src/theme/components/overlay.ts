export const Modal = {
  baseStyle: {
    dialog: {
      bg: "bg.surface",
      boxShadow: "modal",
    },
    overlay: {
      bg: "blackAlpha.600",
    },
  },
};

export const Drawer = {
  baseStyle: {
    dialog: {
      bg: "bg.surface",
    },
    overlay: {
      bg: "blackAlpha.600",
    },
  },
};

export const Popover = {
  baseStyle: {
    content: {
      bg: "bg.surface",
      borderColor: "border.subtle",
      boxShadow: "dropdown",
    },
  },
};

export const Menu = {
  baseStyle: {
    list: {
      bg: "bg.surface",
      borderColor: "border.subtle",
      boxShadow: "dropdown",
    },
    item: {
      _hover: {
        bg: "bg.subtle",
      },
      _focus: {
        bg: "bg.subtle",
      },
    },
  },
};
